
import { LazyDep } from './lazy-dep';

export const enum DB_TYPE {
        postgres = 1,
        sqlite = 2,
        oracledb = 3,
        mssql = 4,
        mongo = 5,
        cassandra = 6
}

export { LazyDep };
